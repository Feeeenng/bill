## React + Ant Design frontend

### Setup

```bash
cd frontend
npm install
```

### Development

```bash
npm run dev
```

The Vite dev server proxies `/api` requests to `http://127.0.0.1:8000`, matching the FastAPI default port.
